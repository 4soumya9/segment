# Segment Builder - React

Tiny React app to create & save simple user/group segments.

Features:

- Right-side popup to add schemas
- User / Group trait dots
- Add, edit, remove schema rows
- Send JSON to webhook
